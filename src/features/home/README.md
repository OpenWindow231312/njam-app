# Home / Search

The landing screen after auth. Recent scans, search across the product catalogue,
and the way into the scanner.

- Reads `scans` (household-scoped history) and `products` / `categories`.
- Search is text over the shared catalogue; results use `ProductCard`.
- The primary action leads to the scanner. Name it "Scan a barcode".
